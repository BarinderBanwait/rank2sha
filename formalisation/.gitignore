# Lean/Lake build artifacts
.lake/

# Blueprint build outputs (rendered locally via blueprint/bp pdf|web)
blueprint/print/
blueprint/web/
blueprint/lean_decls

# OS / editor cruft
.DS_Store

# Secrets / local env
.env

# Logs
*.log

# plasTeX cross-reference cache (regenerated by blueprint/bp web)
blueprint/src/web.paux

# Paper source — belt and braces. Since the 2026-08-29 split this directory is
# tracked by the public-facing repository at the root, which also ignores the
# manuscript. These patterns catch the failure mode that remains possible,
# namely someone copying the manuscript in here.
main.tex
main.pdf
STATE.txt
